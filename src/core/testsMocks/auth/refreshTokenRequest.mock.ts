import { RefreshTokenRequest } from 'src/core/contracts/requests/refresh.request';

export const refreshTokenRequestMock: RefreshTokenRequest = {
    accessToken: 'this is a very long and invalid access token but fear not, it is fake'
};
